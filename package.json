{
  "name": "bobinerie",
  "description": "Le Bobine smart contracts store",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "prepack": "npm run prepack:all",
    "prepack:all": "for dir in contracts/*/; do CONTRACT=$(basename $dir) npm run prepack:contract || exit 1; done",
    "prepack:contract": "test -n \"$CONTRACT\" || (echo '❌ Error: CONTRACT environment variable is required. Usage: CONTRACT=mycontract npm run prepack:contract' && exit 1) && rm -rf ./contracts/$CONTRACT/out && npm run prepack:asc && npm run prepack:tsc",
    "prepack:asc": "deno x asc ./contracts/$CONTRACT/src/mod.ts -o ./contracts/$CONTRACT/out/mod.wasm --runtime stub --enable reference-types",
    "prepack:tsc": "deno x tsc ./contracts/$CONTRACT/src/mod.ts --outFile ./contracts/$CONTRACT/out/mod.d.ts --noCheck --declaration --emitDeclarationOnly",
    "produce": "test -n \"$CONTRACT\" || (echo '❌ Error: CONTRACT environment variable is required. Usage: CONTRACT=mycontract npm run produce' && exit 1) && deno run -A ./run/src/mods/produce/mod.ts ./contracts/$CONTRACT/out/mod.wasm",
    "execute": "deno run -A ./run/src/mods/execute/mod.ts",
    "execute:sign": "deno run -A ./run/src/mods/execute/sign/mod.ts",
    "keygen": "deno run -A ./run/src/mods/keygen/mod.ts",
    "docs": "npm run docs:contracts && npm run docs:catalog",
    "docs:contract": "test -n \"$CONTRACT\" || (echo '❌ Error: CONTRACT environment variable is required. Usage: CONTRACT=mycontract npm run docs:contract' && exit 1) && deno run -A ./run/src/mods/docs/contract/mod.ts",
    "docs:contracts": "for dir in contracts/*/; do CONTRACT=$(basename $dir) npm run docs:contract || exit 1; done",
    "docs:catalog": "deno run -A ./run/src/mods/docs/catalog/mod.ts",
    "lint:code": "biome check .",
    "lint:code:fix": "biome check --write --unsafe .",
    "fmt": "biome format --write .",
    "fmt:check": "biome format .",
    "lint:commits": "commitlint --from origin/main --to HEAD --verbose",
    "test": "NODE_OPTIONS='--import tsx' cucumber-js --import features/support/index.ts",
    "test:verbose": "NODE_OPTIONS='--import tsx' cucumber-js --import features/support/index.ts --format progress-bar --format json:reports/cucumber-report.json"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.10",
    "@commitlint/cli": "^20.2.0",
    "@cucumber/cucumber": "^11.1.0",
    "@hazae41/stdbob": "^0.0.19",
    "@types/node": "^22.10.5",
    "assemblyscript": "^0.28.9",
    "deno": "^2.6.3",
    "ts-node": "^10.9.2",
    "tsx": "^4.19.2",
    "typescript": "^5.9.3"
  },
  "packageManager": "yarn@1.22.22+sha512.a6b2f7906b721bba3d67d4aff083df04dad64c399707841b7acf00f6b133b7ac24255f2652fa22ae3534329dc6180534e98d17432037ff6fd140556e2bb3137e",
  "dependencies": {
    "@hazae41/binary": "^2.0.2",
    "@hazae41/cursor": "^2.1.3"
  }
}
